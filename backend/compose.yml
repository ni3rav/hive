
services:
  # PostgreSQL Database
  # postgres:
  #   image: postgres:17-alpine
  #   container_name: hive-postgres
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: password
  #     POSTGRES_DB: hive
  #     PGDATA: /var/lib/postgresql/data/pgdata
  #   ports:
  #     - '5432:5432'
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ['CMD-SHELL', 'pg_isready -U postgres']
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #   networks:
  #     - hive-network

  # Backend API
  backend:
    image: ni3rav/hive-backend
    container_name: hive-backend
    restart: unless-stopped
    ports:
      - '3000:3000'
    # environment:
    #   - DATABASE_URL=postgresql://postgres:password@postgres:5432/hive
    #   - PORT=3000
    #   - NODE_ENV=production
    #   - FRONTEND_URL=${FRONTEND_URL:-http://localhost:5173}
    #   - RESEND_API_KEY=${RESEND_API_KEY}
    env_file:
      - .env
    # depends_on:
    #   postgres:
    #     condition: service_healthy
    networks:
      - hive-network
    # healthcheck:
    #   test:
    #     [
    #       'CMD',
    #       'node',
    #       '-e',
    #       "require('http').get('http://localhost:3000/health', (r) =>
    #       {process.exit(r.statusCode === 200 ? 0 : 1)})",
    #     ]
    #   interval: 30s
    #   timeout: 3s
    #   start_period: 10s
    #   retries: 3

# volumes:
#   postgres_data:
#     driver: local

networks:
  hive-network:
    driver: bridge
